- @wines.each do |wine|
  .accordion-group
    .accordion-heading
      .row
        .span8
          %a.accordion-toggle{"data-parent" => "#accordion2", "data-toggle" => "collapse", href: "#collapse#{wine.id}"}
            %h4
              .badge= wine.rating
              = "#{wine.vintage} #{wine.name}"
        
    .accordion-body.collapse.in{id: "collapse#{wine.id}" }
      .accordion-inner
        .row
          .span12
            .span6
              %h4 Winemaker notes 
              = wine.winemaker_notes
              %br

              .row
                %h4.span1.blooch Blooches

              - wine.reviews.each do |review|
                %p 
                  .badge= review.rating
                  = review.content
                  = link_to review.reviewer.name, user_path(review.reviewer.name)
                  / = link_to "read more about this frati", wine_path(wine.id), class: "pull-right"
            .span3
              %ul.ca-menu.unstyled
                %li
                  %a{"data-toggle" => "modal", :href => "#myModal", :role => "button"}
                    %span.ca-icon
                      %i.icon-glass
                    / %span.ca-icon Booch about this frati
                  .ca-content
                    %h2.ca-main Leave your blooch
                    %h3.ca-sub Add them to the cloud

                .modal.hide.fade{ id: "myModal", "aria-hidden" => "true", "aria-labelledby" => "myModalLabel", :role => "dialog", :tabindex => "-1"}
                  .modal-header
                    %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", :type => "button"} ×
                    .centered
                      %h3= wine.winery.name
                      %h2
                        = wine.vintage 
                        = wine.name.titleize
                  .modal-body.form-horizontal
                    = simple_form_for @review do |f|
                      = f.error_notification
                      = f.hidden_field :wine_id, value: wine.id
                      = f.input :content, label: "notes about this wine", input_html: { rows: 7 }
                      = f.input :rating, collection: 70..100, label: "points"
                      .modal-footer
                        %button.btn{"aria-hidden" => "true", "data-dismiss" => "modal"} Close
                        = f.button :submit, 'Publish this blooch', :class => 'btn-primary'


                / %li
                /   %a{:href => "#docs"}
                /     %span#heart.ca-icon N
                /   .ca-content
                /     %h2.ca-main Well documented
                /     %h3.ca-sub You'll never be confused again
