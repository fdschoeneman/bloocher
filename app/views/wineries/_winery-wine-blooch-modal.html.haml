%ul.ca-menu.unstyled
  %li
    %a{ "data-toggle" => "modal", :href => "#bloochModal#{wine.id}", :role => "button"}
      %span.ca-icon
        %i.icon-cloud
      .ca-content
        %h2.ca-main Bloochinator
        %h3.ca-sub Blooch on the cloud

  = render partial: 'blooch-modal', object: wine, as: 'wine'
