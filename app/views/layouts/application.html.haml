!!!
%html{:lang => "en"}
  %head
    = render 'head'
  %body{:class => "#{controller_name} #{action_name}"}
    .container
      = render 'navigation'
    .container
      = yield 
    = render 'footer'

    = debug(params) if Rails.env.development?
