%body{:class => "#{controller_name} #{action_name}"}
  = render 'head'
  = render 'navigation'
  = render 'messages'
  .row= yield
  = render 'footer'

  - if Rails.env.development?
    = debug(params) 