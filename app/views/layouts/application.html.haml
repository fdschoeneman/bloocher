= render 'head'
%body{:class => "#{controller_name} #{action_name}"}
  = render 'navigation' #unless controller_name == "wineries"
  = render 'messages'
  = yield
  = render 'footer'
  = javascript_include_tag "application"

  / - if Rails.env.development?
  /  = debug(params) 